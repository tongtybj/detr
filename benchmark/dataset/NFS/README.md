# Prepare for NFS benchmark

## Create directory for dataset

````shell
ln -sfb $PWD/dataset ./dataset # $PWD/dataset is the dierectory to store dataset
````

## Download and unzip dataset

````shell
./download_unpack.sh
````


